'use strict';

angular.module('issueTrackingSystem.issue.issue-service', [])
    .factory('issueService', [
        '$http',
        '$q',
        'BASE_URL',
        function ($http, $q, BASE_URL) {

            return {

            }
        }]);